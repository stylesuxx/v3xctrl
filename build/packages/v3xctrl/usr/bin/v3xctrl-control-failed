#!/bin/bash
set -e

ENV_PATH="/run/v3xctrl.env"
source "${ENV_PATH}"

PWM_THROTTLE="${control_pwm_throttle}"
PWM_STEERING="${control_pwm_steering}"

v3xctrl-pwm-failsafe "$PWM_THROTTLE" "$control_throttle_failsafe" "$PWM_STEERING" "$control_steering_failsafe"
