#!/bin/bash
set -e

ENV_PATH="/run/v3xctrl.env"
source "${ENV_PATH}"

GPIO_THROTTLE="${control_gpio_throttle}"
GPIO_STEERING="${control_gpio_steering}"

pigs s $GPIO_THROTTLE $control_throttle_failsafe
pigs s $GPIO_STEERING $control_steering_failsafe
