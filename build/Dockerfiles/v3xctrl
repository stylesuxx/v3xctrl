FROM arm64v8/debian:bullseye

ENV DEBIAN_FRONTEND=noninteractive

# Install base tools and deps for package build
RUN apt-get update
RUN apt-get install -y \
  lintian \
  ca-certificates

# Copy and install rc-python.deb
COPY rc-python.deb /tmp/
RUN dpkg -i /tmp/rc-python.deb || apt-get install -f -y

# Copy build script and source files
COPY . /src
WORKDIR /src

# Run the build script
RUN ./build/build-v3xctrl-docker.sh
