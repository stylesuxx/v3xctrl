#!/bin/bash
set -e

ENV_PATH="/run/v3xctrl.env"
source "${ENV_PATH}"

GPIO_THROTTLE=18
GPIO_STEERING=13

pigs s $GPIO_THROTTLE $controls_throttle_failsafe
pigs s $GPIO_STEERING $controls_steering_failsafe
